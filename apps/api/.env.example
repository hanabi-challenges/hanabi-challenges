BACKEND_PORT=4000
DATABASE_URL=postgres://hanabi_user:hanabi_password@localhost:55432/hanabi_dev
JWT_SECRET=replace-me-with-a-long-random-string
# Enable simulation mode for local dev — mounts mock hanab-live routes and
# unlocks the "Simulate stage" UI. Never set to true in production.
SIMULATION_MODE=true
HANAB_LIVE_BASE_URL=http://localhost:4000

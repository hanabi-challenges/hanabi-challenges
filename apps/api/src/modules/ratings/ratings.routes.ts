import { Router } from 'express';

// TODO: implement ratings endpoints (T-040–T-041)
// GET /api/stages/:stageId/ratings        — current ELO standings
// GET /api/users/:userId/ratings          — ratings history for a user

const router = Router();

export default router;

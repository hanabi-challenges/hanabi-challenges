import { Router } from 'express';

// TODO: implement result endpoints (T-024–T-028)
// POST /api/results                       — submit game result
// GET  /api/results/:id                   — get result
// DELETE /api/results/:id                 — delete result (admin)
// POST /api/stages/:stageId/gauntlet      — start gauntlet attempt
// POST /api/matches/:matchId/results      — submit match game result

const router = Router();

export default router;

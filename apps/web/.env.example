# Example environment variables for Hanabi Challenges

# PostgreSQL connection URL
# Format: postgres://username:password@host:port/database
DATABASE_URL=postgres://postgres:password@localhost:5432/hanabi_challenges

# JWT secret for signing auth tokens
# Use a long, random string in real environments.
JWT_SECRET=replace_with_a_long_random_secret_string

# Node environment (optional, but common)
# Values: development | test | production
NODE_ENV=development

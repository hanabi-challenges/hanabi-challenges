import type { ReactElement } from 'react';

/**
 * TODO: List / DescriptionList
 * Styled list components for key/value pairs and bulleted/numbered lists.
 */
export function List(): ReactElement | null {
  return null;
}

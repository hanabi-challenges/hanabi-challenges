import type { ReactElement } from 'react';

/**
 * TODO: DatePicker
 * Date selection component (single date for now) that honors locale/formatting tokens.
 */
export function DatePicker(): ReactElement | null {
  return null;
}

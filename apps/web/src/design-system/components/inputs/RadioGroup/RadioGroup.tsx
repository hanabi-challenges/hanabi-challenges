import type { ReactElement } from 'react';

/**
 * TODO: RadioGroup
 * Manages a set of RadioButtons with shared name/value, keyboard navigation, and spacing.
 */
export function RadioGroup(): ReactElement | null {
  return null;
}

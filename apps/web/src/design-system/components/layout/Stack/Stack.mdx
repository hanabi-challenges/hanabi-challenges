import { Meta, Title, Subtitle, Description, Canvas, Story } from '@storybook/addon-docs/blocks';
import * as StackStories from './Stack.stories';

<Meta of={StackStories} />

<Title>Stack</Title>
<Subtitle>Vertical layout with semantic gaps</Subtitle>

<Description>
  This page demonstrates how semantic gaps in the <code>Stack</code> component change depending on
  the content relationship. Two examples are shown: a heading-to-body pair using
  <code>gap="title"</code>, and a body-to-body pair using <code>gap="default"</code>.
</Description>

## Semantic gap comparison

### Heading → body (<code>gap="title"</code>)

<Canvas of={StackStories.HeadingToBody} />

### Body → body (<code>gap="default"</code>)

<Canvas of={StackStories.BodyToBody} />
